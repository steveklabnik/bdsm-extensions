#!/usr/bin/env bash

# TODO: Make version configurable from ~/.bdsm/${extension}/config

define_package \
  name "${extension}" \
  version "6.2" \
  url "http://ftp.gnu.org/pub/gnu/readline"

install_package

succeed "Installed ${package_name} ${package_version}."
