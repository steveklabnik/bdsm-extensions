#!/usr/bin/env bash

modules help

description "${extension} BDSM extension."

actions "install  - Install ${package_name} ${package_version}.
  setup   - setup ${extension} configuration files"

show_help usage

