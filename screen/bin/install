#!/usr/bin/env bash

# TODO: Make version configurable from ~/.bdsm/${extension}/config

define_package \
  name "${extension}" \
  version "4.0.3" \
  archive_format "tar.gz" \
  url "http://mirrors.kernel.org/gnu/${extension}"

install_package

succeed "Installed ${package_name} ${package_version}."
