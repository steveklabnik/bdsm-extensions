#!/usr/bin/env bash

enter "$release_path"

if [[ -s script/dbconsole ]] ; then
  ruby script/dbconsole
elif [[ -s script/rails ]] ; then
  ruby script/rails db
else
  true # TODO: raw psql/mysql/etc... based on database.yml
fi
