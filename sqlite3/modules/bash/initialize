#!/usr/bin/env bash

modules defaults package

read_default version prefix package
read_default base_url prefix package

package_definition \
  dir "${package_name}-autoconf-${package_version}" \
  file "${package_name}-autoconf-${package_version}.tar.gz"

