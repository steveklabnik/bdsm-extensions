#!/usr/bin/env bash

if [[ ${master_pid:-0} -gt 0 ]] ; then
  log "Signaling Nginx ($master_pid) to quit\n(from pidfile: $nginx_pid)"
  kill -QUIT ${master_pid}
else
  log "It appears that Nginx is already not running\n(from pidfile: $nginx_pid)"
fi
