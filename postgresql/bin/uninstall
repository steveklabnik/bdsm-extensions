#!/usr/bin/env bash

source "${extension_modules_path}/cli"

uninstall_package

remove_directories "${prefix_path}/${package_name}"

delete_user "${package_user}"

remove_postgresql_init_script

remove_postgresql_ldconfig

remove_postgresql_profile_d_script

log "Removal of ${package_name} complete."

